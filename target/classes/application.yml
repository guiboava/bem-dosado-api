spring:
  application:
    name: bem-dosado
  datasource:
    url: jdbc:postgresql://localhost:5434/bem_dosado_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
